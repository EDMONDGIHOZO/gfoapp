import React from "react";
import { View, Text } from "react-native";
import GStyles from "../../shared/Gstyle";

const Home = () => {
  return (
    <View style={GStyles.container}>
      <Text style={GStyles.title}> Homepage</Text>
    </View>
  );
};

export default Home;
